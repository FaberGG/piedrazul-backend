spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate

app:
  jwt:
    secret: ${JWT_SECRET}
  cors:
    # TODO: definir dominios frontend reales antes de desplegar.
    # Ejemplo: ["https://app.piedrazul.com", "https://admin.piedrazul.com"]
    allowed-origins: []
